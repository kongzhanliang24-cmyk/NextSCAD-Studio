# Textures 目錄

儀表屏幕貼圖檔案存放處。

## 所需檔案

- `screen_app.png` — P-IMS app 的截圖（圖 8），建議解析度 1024×640 或更高

## 使用方式

1. 保存 app 截圖為 `screen_app.png` 放在此目錄
2. 執行 `python tools/apply_screen_texture.py` 自動將紋理貼到導出的 GLB 檔案屏幕上
